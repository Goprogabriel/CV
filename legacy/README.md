# Previous Astro portfolio

The pre-redesign source and media are preserved here for reference. This directory is excluded from the new Vite build and deployment. Its old dependencies are not installed by the current project. Use git history if you need to restore the previous runnable application.
